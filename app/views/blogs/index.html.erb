<!DOCTYPE html>
<html>
<head>
  <meta charset= "UTF-8"/>
  <title>blog</title>
  <link rel="stylesheet" href="normaraize.css">
  <link rel="stylesheet" href="style.css">
</head>

<body>

  <section>
    <% @articles.each do |article| %>
      <li>
        <h2><%= article.title %></h2>
        <% if article.image != "" %>
          <p><img src="<%= article.image %>"></p>
        <% end %>
        <p><%= article.content %></p>
        <p><%= article.created_at %></p>
        <p><%= button_to '編集', "/#{article.id}/edit", method: :get, class: 'button' %></p>
        <p><%= button_to '削除', "/#{article.id}", method: :delete, class: 'button' %></p>
      </li>
      <% end %>
  </section>

</body>
</html>